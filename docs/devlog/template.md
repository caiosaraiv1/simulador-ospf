### 🗓️ DD/MM/AAAA

- **O que fiz:** - [Ex: Consegui fazer o parse do `topologia.json` usando o nlohmann/json. As instâncias dos Roteadores já estão sendo populadas na memória.]
- **Onde parei / Próximo passo:** - [Ex: Parei na hora de instanciar a classe FilaMensagens. Amanhã preciso implementar o std::mutex para garantir que o push_back seja thread-safe.]
- **Bloqueios / Bugs:** - [Ex: O CMake não estava achando o pacote do JSON. Descobri que precisava adicionar `find_package(nlohmann_json REQUIRED)` no CMakeLists.txt.]
